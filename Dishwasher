repeat
  placeDishesInMachine();
until (machine == FULL || NumCleanDish == 0);

addTablet();
selectProgram("45´");
turnOnWater();

if (washing == 1) {
	time = CheckTimeRemaining();
	returnAfter(time);
} else {
	EmptyMachine();
}
